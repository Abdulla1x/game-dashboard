---
name: Bug report
about: Something is detected wrongly, missing, or will not launch
labels: bug
---

**What happened**

**What you expected**

**Which game, if it is about one**
Name, and where it is installed (which launcher, or a folder path).

**Scan output**
Run `py scan.py --dry-run -v` and paste the relevant lines. That prints your whole
library, so please trim it rather than pasting all of it.

```
paste here
```

**Environment**
- Windows version:
- Python version (`py --version`):
- Installed via `py install.py`, or run from source:
